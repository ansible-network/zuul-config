---
- project:
    check:
      jobs:
        - linters
    gate:
      jobs:
        - linters

- project:
    name: ansible/ansible
    default-branch: devel

- project:
    name: ansible-network/cloud-vpn
    check:
      jobs:
        - linters
        - cloud-vpn-aws-to-aws
    gate:
      jobs:
        - linters
        - cloud-vpn-aws-to-aws
