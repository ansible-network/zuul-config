---
- project:
    name: github.com/ansible-network/ansible-zuul-jobs
    check:
      jobs:
        - noop
    gate:
      jobs:
        - noop

- project:
    name: github.com/ansible-network/zuul-config
    check:
      jobs:
        - noop
    gate:
      jobs:
        - noop

